var req = require('request');

var uri = process.env.u || 'http://localhost:5984';

req({uri:uri}, function(er, resp, body) {
  if(er)
    return console.error(er.stack);

  console.log('=== Done ===');
  console.dir({code: resp.statusCode, body:body});
})
